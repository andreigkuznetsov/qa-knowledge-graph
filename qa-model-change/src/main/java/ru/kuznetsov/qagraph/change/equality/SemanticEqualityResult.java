package ru.kuznetsov.qagraph.change.equality;

/**
 * Result of Canonical QA Model semantic state comparison.
 */
public enum SemanticEqualityResult {
    SEMANTICALLY_EQUAL,
    SEMANTICALLY_UNEQUAL,
    UNSUPPORTED
}
