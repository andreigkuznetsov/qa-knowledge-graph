package ru.kuznetsov.qaip.simulation;

/**
 * Public simulation entry-point type.
 *
 * <p>The successful {@code simulate(...)} operation is intentionally added
 * only when candidate materialization can produce a valid
 * {@code SimulationResult}. Phases 1 and 2 expose no incomplete operation.</p>
 */
public final class ModelSimulationEngine {
}
